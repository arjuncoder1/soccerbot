[project]
name = "soccerbot-workspace"
version = "0.1.0"
description = "Soccerbot workspace"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "training",
    "thirdparty/lerobot",
    "soccerbot",
    "vla-inference",
]

[tool.uv]
package = false
